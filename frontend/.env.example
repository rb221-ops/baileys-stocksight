# Frontend environment variables
# Copy this to .env.local

# Point this at your deployed backend URL
# For local dev, leave as-is (Vite proxies /api → localhost:8000)
VITE_API_URL=https://your-backend.railway.app/api
